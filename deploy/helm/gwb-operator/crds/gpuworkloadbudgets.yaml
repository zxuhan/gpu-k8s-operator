apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gpuworkloadbudgets.budget.zxuhan.dev
spec:
  group: budget.zxuhan.dev
  names:
    kind: GPUWorkloadBudget
    listKind: GPUWorkloadBudgetList
    plural: gpuworkloadbudgets
    shortNames:
    - gwb
    - gwbs
    singular: gpuworkloadbudget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.consumedGpuHours
      name: Consumed
      type: string
    - jsonPath: .status.remainingGpuHours
      name: Remaining
      type: string
    - jsonPath: .spec.quota.windowHours
      name: Window
      type: integer
    - jsonPath: .spec.enforcement.action
      name: Action
      type: string
    - jsonPath: .status.trackedPods
      name: Pods
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GPUWorkloadBudget is the Schema for the gpuworkloadbudgets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GPUWorkloadBudget
            properties:
              enforcement:
                description: enforcement describes what the controller does once the
                  quota is exceeded.
                properties:
                  action:
                    description: |-
                      action selects the behaviour applied to offending pods. Exactly one of
                      Evict, Pause, or AlertOnly.
                    enum:
                    - Evict
                    - Pause
                    - AlertOnly
                    type: string
                  gracePeriodSeconds:
                    default: 30
                    description: |-
                      gracePeriodSeconds is how long a pod is allowed to remain running after being
                      flagged for enforcement. For Evict, this is passed to the eviction subresource.
                      For Pause, the pause annotation is delayed this long. AlertOnly ignores it.
                    format: int32
                    minimum: 0
                    type: integer
                required:
                - action
                type: object
              gpuResourceName:
                default: nvidia.com/gpu
                description: |-
                  gpuResourceName is the extended resource counted against the budget.
                  Defaults to nvidia.com/gpu. When pods do not request this resource,
                  the controller falls back to CPU-second simulation (see docs/accounting-model.md).
                type: string
              quota:
                description: quota is the cap, in GPU-hours, over the rolling window.
                properties:
                  gpuHours:
                    anyOf:
                    - type: integer
                    - type: string
                    description: |-
                      gpuHours is the maximum cumulative GPU-hours permitted in the rolling window.
                      Accepts fractional values (e.g. "0.5", "100", "250.75").
                      Must be > 0; zero or negative values are rejected by the validating webhook.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  windowHours:
                    description: windowHours is the length of the rolling window,
                      in hours. Must be at least 1.
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - gpuHours
                - windowHours
                type: object
              selector:
                description: |-
                  selector picks the pods whose GPU consumption counts against this budget.
                  An empty selector is rejected by the validating webhook — the Kubernetes
                  default of "empty matches everything" would silently turn a misconfigured
                  budget into a namespace-wide accounting sink.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - enforcement
            - quota
            - selector
            type: object
          status:
            description: status defines the observed state of GPUWorkloadBudget
            properties:
              conditions:
                description: conditions carries Ready, QuotaExceeded, and Degraded.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consumedGpuHours:
                anyOf:
                - type: integer
                - type: string
                description: |-
                  consumedGpuHours is the cumulative GPU-hours counted against this budget
                  over the current rolling window.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              lastEnforcementAt:
                description: |-
                  lastEnforcementAt is the wall-clock time the controller last took an
                  enforcement action against any pod under this budget. Nil until the first
                  action fires.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the .metadata.generation the controller
                  last reconciled.
                format: int64
                type: integer
              remainingGpuHours:
                anyOf:
                - type: integer
                - type: string
                description: remainingGpuHours is spec.quota.gpuHours minus consumedGpuHours,
                  floored at zero.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              trackedPods:
                description: |-
                  trackedPods is the number of pods currently matched by the selector. Reported
                  so operators can spot an empty selector before it silently fails to account.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
